# Please adjust parameters to your PostgreSQL Instance
# Make sure you pass DATABASE_NAME to existing database
# which can be created in PostgreSQL with command:
# create database scrap;
# or using GUI database explorers

USERNAME = "postgres"
PASSWORD = "postgres"
HOST = "localhost"
DATABASE_NAME = "scrap3"
